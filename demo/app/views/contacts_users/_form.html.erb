<%= form_with(model: contacts_user, local: true) do |form| %>
  <% if contacts_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contacts_user.errors.count, "error") %> prohibited this contacts_user from being saved:</h2>

      <ul>
        <% contacts_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :contact_id %>
    <%= form.number_field :contact_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
