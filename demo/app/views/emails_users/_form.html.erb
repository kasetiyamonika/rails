<%= form_with(model: emails_user, local: true) do |form| %>
  <% if emails_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(emails_user.errors.count, "error") %> prohibited this emails_user from being saved:</h2>

      <ul>
        <% emails_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :email_id %>
    <%= form.number_field :email_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
